@{
    ViewData["Title"] = "Home Page";
}

@model Product

<section class="py-5">
    <div class="container px-4 px-lg-5 my-5">
        <div class="row gx-4 gx-lg-5 align-items-center">
            <div class="col-md-6"><img class="card-img-top mb-5 mb-md-0" src="@Model.img_link" alt="..." /></div>
            <div class="col-md-6">
                <div class="small mb-1">@Model.category</div>
                <h1 class="display-5 fw-bolder">@Model.name</h1>
                <div class="fs-5 mb-5">
                    <span>$@Model.price.00</span>
                </div>
                <p class="lead">@Model.description</p>
                <div class="d-flex">
                    <input class="form-control text-center me-3" id="inputQuantity" type="num" value="1" style="max-width: 3rem" />
                    <button class="btn btn-outline-dark flex-shrink-0" type="button">
                        <i class="bi-cart-fill me-1"></i>
                        Add to cart
                    </button>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Related items section-->
<section class="py-5 bg-light">
    <div class="container px-4 px-lg-5 mt-5">
        <h2 class="fw-bolder mb-4">You may also enjoy:</h2>
        @* <div class="row gx-4 gx-lg-5 row-cols-1 row-cols-md-2 row-cols-xl-3 justify-content-center"> *@
        @*     @foreach (var product in Model) *@
        @*     {  *@
        @*         <partial name="ProductCard" model="product"/> *@
        @*     } *@
        @* </div> *@
    </div>
</section>
